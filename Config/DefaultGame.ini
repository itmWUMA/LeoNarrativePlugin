# LeoNarrative 插件默认配置
# 剧本目录 staging：把 <工程>/Content/Scripts 整目录打进 pak（UFS 可读），
# 运行时经 FPaths::ProjectContentDir()/Scripts 解析——打包与编辑器行为一致。
# 注意：打包设置读取宿主工程配置最可靠，宿主 DefaultGame.ini 需含同样条目（见 AGENTS.md）。

[/Script/UnrealEd.ProjectPackagingSettings]
+DirectoriesToAlwaysStageAsUFS=(Path="Scripts")
